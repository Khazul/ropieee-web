post_install()
{
   systemctl daemon-reload
}

post_upgrade()
{
#   cd /opt/RoPieee/web
#   npm cache verify
#   rm -rf /opt/RoPieee/web/node_modules
#   npm install

   systemctl daemon-reload
   if systemctl is-enabled ropieee-web
   then
     systemctl restart ropieee-web
   fi
}
